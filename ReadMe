The folder contains source code of two microservices. It also contains APIs file and screenshots of UI of app. 

Steps to run:
1) Import the code of ticket service as maven project - will run at - localhost:1000
2) Import the code of empoyee service as maven project - will run at - localhost:2000
3) Compile angular app code on your local and make it up - will run at - localhost:4200
4) Now open login page of app - http://localhost:4200/login
5) Check the log files incase of any error - will generate in current user directory


######################################For Employee-Ticket Portal:################################################
I have created two independent services -
1) EmployeeService 
2) Ticket Service. 

Employee service APIs are exposed to public and ticket service is internally used by employee service thorugh RestTemplate.
For UI, I have used angular which consumes APIs from Employee service. 

###########################GithubLink for Employee-Ticket-Portal##################################################
https://github.com/Vivek22409/employee-ticket-service

#########################ActuatorEndPoints########################################################################
http://localhost:1000/actuator

#########################SwaggerEndpoints#########################################################################
http://localhost:1000/v2/api-docs



